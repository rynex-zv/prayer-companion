# app.host

Native host role for phone and Windows.

Use this folder for MAUI shell code, native permissions, notifications, alerts, native storage, calls, email, and hosted web assets.

Keep business logic in `core`.
