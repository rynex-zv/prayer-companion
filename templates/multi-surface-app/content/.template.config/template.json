{
  "$schema": "http://json.schemastore.org/template",
  "author": "Rynex",
  "classifications": ["MAUI", "Web", "WASM", "React", "Template"],
  "identity": "Rynex.MultiSurfaceApp",
  "name": "Multi-surface App",
  "shortName": "multi-surface-app",
  "sourceName": "TemplateApp",
  "preferNameDirectory": true,
  "description": "Creates a Core + Web Client + Web Bridge + Native Host project family with generic role folders.",
  "tags": {
    "language": "C#",
    "type": "project"
  },
  "symbols": {
    "projectType": {
      "type": "parameter",
      "datatype": "choice",
      "displayName": "Project type",
      "description": "Choose which role folders to create.",
      "defaultValue": "full-app",
      "choices": [
        {
          "choice": "full-app",
          "displayName": "Full App",
          "description": "Creates core, web.client, web.bridge, app.host, and core.tests."
        },
        {
          "choice": "web-only",
          "displayName": "Web Only",
          "description": "Creates core, web.client, web.bridge, and core.tests."
        },
        {
          "choice": "native-host-only",
          "displayName": "Native Host Only",
          "description": "Creates core, app.host, and core.tests."
        }
      ]
    },
    "widgetSupport": {
      "type": "parameter",
      "datatype": "choice",
      "displayName": "Widget support",
      "description": "Include platform-neutral widget contracts, layout resolution, preview, and renderer extension points.",
      "defaultValue": "none",
      "choices": [
        { "choice": "none", "displayName": "None", "description": "Does not add widget code or UI." },
        { "choice": "cross-platform", "displayName": "Cross-platform", "description": "Adds shared widget contracts and opt-in renderer extension points." }
      ]
    }
  },
  "sources": [
    {
      "source": "./",
      "target": "./",
      "exclude": [
        ".template.config/**",
        "**/[Bb]in/**",
        "**/[Oo]bj/**",
        "web.client/node_modules/**",
        "web.client/dist/**",
        "web.client/assets/**"
      ],
      "modifiers": [
        {
          "condition": "(projectType == \"web-only\")",
          "exclude": ["app.host/**"]
        },
        {
          "condition": "(projectType == \"native-host-only\")",
          "exclude": ["web.client/**", "web.bridge/**"]
        },
        {
          "condition": "(widgetSupport != \"cross-platform\")",
          "exclude": ["core/Widgets/**", "core.tests/Widgets/**", "web.client/src/widgets/**", "app.host/Widgets/**"]
        }
      ]
    }
  ],
  "primaryOutputs": [
    {
      "path": "README.md"
    }
  ]
}
